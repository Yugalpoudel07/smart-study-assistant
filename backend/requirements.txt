# Smart Study Assistant — Render-optimised requirements
# Total install size: ~80 MB (vs ~4 GB with local models)

# Web framework
fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.10.6

# Anthropic Claude API — replaces spaCy + Flan-T5 entirely
anthropic==0.57.0

# PDF generation
fpdf2==2.8.3

# File upload support (if needed later)
python-multipart==0.0.6
